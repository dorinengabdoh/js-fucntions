// calculateSupply
let age=1;
let Q=2;
function calculateSupply(age, Q){
   V=Q*age;
   console.log("You will need" ,Q, "to last you until the ripe old age of",age);
}
calculateSupply(20, 15);
calculateSupply(2, 1);
calculateSupply(2, 5);